   <!-- main -->
  <div class="contents">
    <!-- user info -->
    <div class="user">
      <div class="user-headline">
        <h2>User info</h2>
     <!-- users.info -->


      </div>
    </div>
<!-- books -->
<div class="book">
  <h2>Book detail</h2>
<table width: "100%" >
  <tr>
    <th　width: "40px"></th>
    <th width: "40px">Title</th>
    <th width: "50px">opinion</th>
  </tr>
 <tr>
  <td>image</td>
  <td><%= @book.title %></td>
  <td><%= @book.body %></td>
  <td>
    <%= link_to'Edit', edit_book_path(@book) %>
    <%= link_to 'Destroy', books_path(@book.id), method: :delete %>
    </td>
 </tr>
</table>
</div>
</div>

<!-- new books -->
<div class="new-book">
<h2>New book</h2>
<!-- form_with -->
<%= form_with model:@book, local:true do|f| %>

<h4>Title</h4>
<%= f.text_field :title %>

<h4>Opinion</h4>
<%= f.text_area :body %>

<%= f.submit 'Create Book' %>

<% end %>

</div>
</div>